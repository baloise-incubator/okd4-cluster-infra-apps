apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openhands
spec:
  host: openhands.baloise.dev
  port:
    targetPort: 8443
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: openhands-oauth-proxy
    weight: 100
