apiVersion: v1
kind: Service
metadata:
  name: openhands
spec:
  selector:
    vm.kubevirt.io/name: openhands
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
