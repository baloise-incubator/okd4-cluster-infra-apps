apiVersion: apps/v1
kind: Deployment
metadata:
  name: librechat
  labels:
    app: librechat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: librechat
  template:
    metadata:
      labels:
        app: librechat
      annotations:
        reloader.stakater.com/auto: "true"
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: librechat
          # NOTE: verify non-root OKD compatibility before going live (open question #3).
          # If the official image fails due to root-owned paths, apply emptyDir mounts
          # for /app/uploads, /app/logs, /app/client/dist and /app/.env equivalents.
          image: ghcr.io/danny-avila/librechat:latest
          ports:
            - name: http
              containerPort: 3080
              protocol: TCP
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "3080"
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: mongodb-password
            - name: MONGO_URI
              value: "mongodb://librechat:$(MONGODB_PASSWORD)@mongodb:27017/librechat?authSource=admin"
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: litellm-api-key
            - name: LITELLM_MODEL
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: litellm-model
            - name: OPENID_CLIENT_ID
              value: "librechat"
            - name: OPENID_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: oidc-client-secret
            - name: OPENID_ISSUER
              value: "https://librechat-dex.baloise.dev"
            - name: OPENID_SCOPE
              value: "openid profile email groups"
            - name: OPENID_CALLBACK_URL
              value: "https://librechat.baloise.dev/oauth/openid/callback"
            - name: OPENID_BUTTON_LABEL
              value: "Login with OpenShift"
            - name: OPENID_SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: openid-session-secret
            - name: ALLOW_SOCIAL_LOGIN
              value: "true"
            - name: ALLOW_SOCIAL_REGISTRATION
              value: "true"
            - name: CREDS_KEY
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: creds-key
            - name: CREDS_IV
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: creds-iv
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: jwt-secret
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: librechat-secrets
                  key: jwt-refresh-secret
          volumeMounts:
            - name: config
              mountPath: /app/librechat.yaml
              subPath: librechat.yaml
              readOnly: true
            - name: uploads
              mountPath: /app/uploads
            - name: logs
              mountPath: /app/logs
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
      volumes:
        - name: config
          configMap:
            name: librechat-config
        - name: uploads
          emptyDir: {}
        - name: logs
          emptyDir: {}
