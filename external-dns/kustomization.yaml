apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns

resources:
  - https://github.com/kubernetes-sigs/external-dns/kustomize?ref=v0.20.0
  - https://raw.githubusercontent.com/kubernetes-sigs/external-dns/v0.20.0/config/crd/standard/dnsendpoints.externaldns.k8s.io.yaml
  - base/cf-api-key.yaml

patchesStrategicMerge:
  - overlays/deployment.yaml
  - overlays/clusterrole.yaml
