apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: grafana-tls
  name: grafana-oauth
spec:
  ports:
  - name: web
    port: 3000
    targetPort: https
  selector:
    app: grafana-oauth

